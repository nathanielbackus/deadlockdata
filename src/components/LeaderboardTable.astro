---
import Leaderboard from "../components/Leaderboard.jsx";
const { request } = Astro;
const url = new URL(request.url);
const region = url.searchParams.get("region") || "all";
---
<Leaderboard client:visible initialRegion={region} />
